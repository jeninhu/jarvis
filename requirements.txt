# Dependências essenciais
Flask==3.1.0
Flask-Cors==5.0.0
openai==0.27.4  # Atualizado para a última versão estável compatível
psycopg2==2.9.10
pytz==2024.1
Werkzeug==3.1.3

# Dependências opcionais (adicione apenas se necessário)
# tqdm==4.66.5  # Para barras de progresso
# python-Levenshtein==0.25.1  # Para comparação de strings (se usado no projeto)
# pandas==2.2.2  # Manipulação de dados
# numpy==2.0.1  # Manipulação de arrays numéricos
# Unidecode==1.3.8  # Para remover acentos de texto (se usado no processamento)
